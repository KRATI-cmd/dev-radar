{
  "rewrites": [
    { "source": "/api/((?!cron/).*)", "destination": "/api/index" }
  ],
  "functions": {
    "api/**/*.ts": {
      "maxDuration": 60,
      "includeFiles": "{config,seed}/**"
    }
  },
  "crons": [
    { "path": "/api/cron/daily", "schedule": "0 8 * * *" }
  ]
}
